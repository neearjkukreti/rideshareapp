app.service("myride",function user($http){
    this.offeredRides ={};
    this.appliedRides ={};
    
 /*Define class variable*/
 
 /*Define class methods*/
 
    // This function is used to retrieve the offered ride of the host(user)
    this.getOfferedRide = function () {
        
        //console.log(this.currentRide);
    }
    // This function is used to retrieve the applied ride by the user
    this.getAppliedRide = function () {
        //console.log(this.currentRide);
    }
  
});